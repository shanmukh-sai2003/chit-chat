import ChatList from "./ChatList";

function MainPage() {
    return (
        <main className="bg-black h-[100vh] w-[100vw] text-white">
            <ChatList />
        </main>
    );
}

export default MainPage;